import 'package:flutter/material.dart';

class FormFillView extends StatelessWidget {
  const FormFillView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Form Fill For Resume"),
      ),
    );
  }
}
